substitutions:
  # Phases of the Voice Assistant
  # The voice assistant is ready to be triggered by a wake word
  voice_assist_idle_phase_id: '1'
  # The voice assistant is waiting for a voice command (after being triggered by the wake word)
  voice_assist_waiting_for_command_phase_id: '2'
  # The voice assistant is listening for a voice command
  voice_assist_listening_for_command_phase_id: '3'
  # The voice assistant is currently processing the command
  voice_assist_thinking_phase_id: '4'
  # The voice assistant is replying to the command
  voice_assist_replying_phase_id: '5'
  # The voice assistant is not ready
  voice_assist_not_ready_phase_id: '10'
  # The voice assistant encountered an error
  voice_assist_error_phase_id: '11'

globals:
  # Global variable tracking the phase of the voice assistant (defined above). Initialized to not_ready
  - id: voice_assistant_phase
    type: int
    restore_value: no
    # initial_value: ${voice_assist_not_ready_phase_id}

microphone:
  - platform: satellite1
    id: sat1_mics
    sample_rate: 48000
    i2s_din_pin: GPIO15
    bits_per_sample: 32bit
    channel: stereo
    pdm: false
    adc_type: external
    i2s_audio_id: i2s_shared

micro_wake_word:
  id: mww
  models:
    - model: https://fph-firmware-assets.s3.us-east-1.amazonaws.com/wake-word/hey_jarvis.json
      id: hey_jarvis
    - model: https://fph-firmware-assets.s3.us-east-1.amazonaws.com/wake-word/okay_nabu.json
      id: okay_nabu
    - model: https://fph-firmware-assets.s3.us-east-1.amazonaws.com/wake-word/stop.json
      id: stop
      internal: true
  vad:
  microphone:
    microphone: sat1_mics
    channels: 1
    # gain_factor substitution lets the flash YAML tune wakeword recall
    # without forking this file. Default 6 matches the FPH stock build.
    gain_factor: ${mic_gain_factor}
  stop_after_detection: false
  on_wake_word_detected:
    # If the wake word is detected when the device is muted (Possible with the software mute switch): Do nothing
    - if:
        condition:
          switch.is_off: master_mute_switch
        then:
          # If a timer is ringing: Stop it, do not start the voice assistant (We can stop timer from voice!)
          - if:
              condition:
                switch.is_on: timer_ringing
              then:
                - switch.turn_off: timer_ringing
              # Stop voice assistant if running
              else:
                - if:
                    condition:
                      voice_assistant.is_running:
                    then:
                      voice_assistant.stop:
                    # Stop any other media player announcement
                    else:
                      - if:
                          condition:
                            media_player.is_announcing:
                              id: external_media_player

                          then:
                            - media_player.stop:
                                announcement: true
                                id: external_media_player
                          # Start the voice assistant and play the wake sound, if enabled
                          else:
                            - if:
                                condition:
                                  switch.is_on: wake_sound
                                then:
                                  - script.execute:
                                      id: play_sound
                                      priority: true
                                      sound_file: "wake_word_triggered_sound"
                                  # External routing has to wait long enough
                                  # for the wake chime to actually land on
                                  # the MA speaker before voice_assistant
                                  # starts — otherwise the satellite's mic
                                  # opens while the chime is still playing
                                  # remotely and STT picks it up.
                                  - if:
                                      condition:
                                        lambda: 'return id(external_speaker_routing_on);'
                                      then:
                                        - delay: ${external_speaker_wake_sound_delay}
                                      else:
                                        - delay: 300ms
                            - voice_assistant.start:
                                wake_word: !lambda return wake_word;

voice_assistant:
  id: va
  microphone:
    microphone: sat1_mics
    channels: 0

  media_player: external_media_player
  micro_wake_word: mww
  use_wake_word: false
  # Substitution-driven so a flash YAML can override without forking. XMOS
  # already does NS / AGC in hardware so all three default to "off" — the
  # software-side stack is layered for rooms where the hardware tuning
  # isn't enough. See satellite1.base.yaml's `Wakeword / STT tuning` block.
  noise_suppression_level: ${va_noise_suppression_level}
  auto_gain: ${va_auto_gain}
  volume_multiplier: ${va_volume_multiplier}
  on_client_connected:
    - lambda: id(init_in_progress) = false;
    - micro_wake_word.start:
    - lambda: id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
    - script.execute: control_leds
  on_client_disconnected:
    - voice_assistant.stop:
    - lambda: id(voice_assistant_phase) = ${voice_assist_not_ready_phase_id};
    - script.execute: control_leds
    # Defensive cleanup: if HA disconnects while we had the external player
    # ducked for listening, vad_end will never fire on the satellite side
    # and the player would stay at 5% until the HA-side watchdog runs.
    # Restore on disconnect so the music recovers immediately whenever
    # the API link comes back. The restore script is itself a no-op if
    # routing is off, so this is safe to call unconditionally.
    - script.execute: restore_external_after_listening
    - lambda: 'id(external_tts_mirrored_this_cycle) = false;'
  on_error:
    - if:
        condition:
          and:
            - lambda: return !id(init_in_progress);
            - lambda: return code != "duplicate_wake_up_detected";
        then:
          - lambda: id(voice_assistant_phase) = ${voice_assist_error_phase_id};
          - script.execute: control_leds
    # Stop ducking audio.
    - mixer_speaker.apply_ducking:
        id: media_mixing_input
        decibel_reduction: 0   # 0 dB means no reduction
        duration: 1.0s
    # If we ducked the external player for listening and then errored before
    # vad_end fired, restore so the player isn't left stuck at 5%.
    - script.execute: restore_external_after_listening
    - lambda: 'id(external_tts_mirrored_this_cycle) = false;'

  # When the voice assistant starts: Play a wake up sound, duck audio.
  on_start:
    - mixer_speaker.apply_ducking:
        id: media_mixing_input
        decibel_reduction: 20   # Number of dB quieter; higher implies more quiet, 0 implies full volume
        duration: 0.0s          # The duration of the transition (default is 0)
    # Fresh pipeline cycle — clear the "TTS already mirrored" guard so
    # whichever trigger ends up carrying the URL (intent_progress for
    # streaming, tts_end for everything else) gets to mirror exactly once.
    - lambda: 'id(external_tts_mirrored_this_cycle) = false;'
  on_listening:
    - lambda: id(voice_assistant_phase) = ${voice_assist_waiting_for_command_phase_id};
    - script.execute: control_leds
    # External routing: duck the MA player to 5% so TTS tails / music can't
    # bleed into the satellite mic during STT. Restored at on_stt_vad_end /
    # on_end / on_error. No-op when routing is off or entity is unset.
    - script.execute: duck_external_for_listening
  on_stt_vad_start:
    - lambda: id(voice_assistant_phase) = ${voice_assist_listening_for_command_phase_id};
    - script.execute: control_leds
  on_stt_vad_end:
    - lambda: id(voice_assistant_phase) = ${voice_assist_thinking_phase_id};
    - script.execute: control_leds
    # User has stopped speaking — restore the external player volume so
    # background music / next-turn TTS resumes at its normal level. We
    # intentionally restore here (not at on_end) per the framework spec:
    # the duck is scoped to *listening*, not the whole assist cycle.
    - script.execute: restore_external_after_listening
  on_intent_progress:
    - if:
        condition:
          # A nonempty x variable means a streaming TTS url was sent to the media player
          lambda: 'return !x.empty();'
        then:
          - lambda: id(voice_assistant_phase) = ${voice_assist_replying_phase_id};
          - script.execute: control_leds
          # Start a script that would potentially enable the stop word if the response is longer than a second
          - script.execute: activate_stop_word_once
          # When external speaker routing is on, mirror the streaming TTS URL
          # to the configured Music Assistant / HA media_player. The local
          # speaker_source media_player keeps decoding the stream (silent at
          # the DAC) so on_end / on_listening transitions stay accurate; the
          # external entity is fed the same URL with announce: true so MA's
          # native announcement-volume handling kicks in.
          - script.execute:
              id: play_on_external
              url: !lambda 'return x;'
          - lambda: 'id(external_tts_mirrored_this_cycle) = true;'
  on_tts_start:
    - if:
        condition:
          # The intent_progress trigger didn't start the TTS Reponse
          lambda: 'return id(voice_assistant_phase) != ${voice_assist_replying_phase_id};'
        then:
          - lambda: id(voice_assistant_phase) = ${voice_assist_replying_phase_id};
          - script.execute: control_leds
          # Start a script that would potentially enable the stop word if the response is longer than a second
          - script.execute: activate_stop_word_once
  # NOTE: on_tts_start's `x` is the spoken TEXT, not a URL
  # (voice_assistant.cpp:730 fires tts_start_trigger with the response
  # text). We can't mirror here — see on_tts_end below, where x IS a URL
  # for both the assist pipeline and the announce / start_conversation
  # service path.
  on_tts_end:
    # `x` is the TTS URL on every TTS flow this firmware sees:
    #   - assist pipeline streaming: tts_end fires after intent_progress
    #     already mirrored — the dedup flag suppresses a double-play.
    #   - assist pipeline non-streaming: intent_progress fired with an
    #     empty URL so no mirror happened; we mirror here.
    #   - assist_satellite.announce / start_conversation: intent_progress
    #     never fires; tts_end is the only place the URL surfaces
    #     (voice_assistant.cpp:949).
    - if:
        condition:
          and:
            - lambda: 'return !x.empty();'
            - lambda: 'return !id(external_tts_mirrored_this_cycle);'
        then:
          - script.execute:
              id: play_on_external
              url: !lambda 'return x;'
          - lambda: 'id(external_tts_mirrored_this_cycle) = true;'
  # When the voice assistant ends ...
  on_end:
    - wait_until:
        not:
          voice_assistant.is_running:
    # Stop ducking audio.
    - mixer_speaker.apply_ducking:
        id: media_mixing_input
        decibel_reduction: 0   # 0 dB means no reduction
        duration: 1.0s
    # Stop the script that would potentially enable the stop word if the response is longer than a second
    - script.stop: activate_stop_word_if_tts_step_is_long
    # Disable the stop word (If the tiemr is not ringing)
    - if:
        condition:
          switch.is_off: timer_ringing
        then:
    # - lambda: id(stop).disable();
    # If the end happened because of an error, let the error phase on for a second
    - if:
        condition:
          lambda: return id(voice_assistant_phase) == ${voice_assist_error_phase_id};
        then:
          - delay: 1s
    # Reset the voice assistant phase id and reset the LED animations.
    - lambda: id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
    - script.execute: control_leds
    # Defensive restore: on_stt_vad_end usually un-ducks the external player,
    # but if the VA was cancelled mid-listening (action button, stop word,
    # client disconnect) vad_end never fired and the player would stay
    # stuck at 5%. The restore script is idempotent so calling it again
    # after a normal cycle is harmless.
    - script.execute: restore_external_after_listening
    # Reset the TTS-mirrored guard for the next cycle. Critical for the
    # assist_satellite.announce / start_conversation path because that
    # path doesn't fire on_start (no RUN_START event), so on_end is the
    # only reliable reset point for those cycles.
    - lambda: 'id(external_tts_mirrored_this_cycle) = false;'

  on_timer_finished:
    - switch.turn_on: timer_ringing
  on_timer_started:
    - script.execute: control_leds
  on_timer_cancelled:
    - script.execute: control_leds
  on_timer_updated:
    - script.execute: control_leds
  on_timer_tick:
    - script.execute: control_leds

script:
  # Script used activate the stop word if the TTS step is long.
  # Why is this wrapped on a script?
  #   Becasue we want to stop the sequence if the TTS step is faster than that.
  #   This allows us to prevent having the deactivation of the stop word before its own activation.
  - id: activate_stop_word_if_tts_step_is_long
    then:
      - delay: 1s
      # Enable stop wake word
      # - lambda: id(stop).enable();

  - id: activate_stop_word_once
    then:
      - delay: 1s
      # Enable stop wake word — but ONLY if external speaker routing is
      # off. With routing on, the XMOS AEC reference is the local muted
      # stream, NOT what the external MA player actually emits, so the
      # satellite mic hears its own TTS uncancelled and the "stop" model
      # false-triggers on the satellite's own response (cancels TTS
      # mid-sentence). Skip until Phase 3 XMOS firmware can route AEC
      # reference from the actual playback source.
      - if:
          condition:
            and:
              - switch.is_off: timer_ringing
              - lambda: 'return !id(external_speaker_routing_on);'
          then:
            - micro_wake_word.enable_model: stop
            - wait_until:
                not:
                  media_player.is_announcing:
                    id: external_media_player
            - if:
                condition:
                  switch.is_off: timer_ringing
                then:
                  - micro_wake_word.disable_model: stop

select:
  - platform: template
    id: mww_sensitivity_select
    name: "Wake word sensitivity"
    optimistic: true
    initial_option: Slightly sensitive
    restore_value: true
    entity_category: config
    options:
      - Slightly sensitive
      - Moderately sensitive
      - Very sensitive
    on_value:
      # Sets specific wake word probabilities computed for each particular model
      # Note probability cutoffs are set as a quantized uint8 value, each comment has the corresponding floating point cutoff
      # False Accepts per Hour values are tested against all units and channels from the Dinner Party Corpus.
      # These cutoffs apply only to the specific models included in the firmware: okay_nabu@20241226.3, hey_jarvis@v2, hey_mycroft@v2
      lambda: |-
        if (x == "Slightly sensitive") {
          id(okay_nabu).set_probability_cutoff(217);    // 0.85 -> 0.000 FAPH on DipCo (Manifest's default)
          id(hey_jarvis).set_probability_cutoff(247);   // 0.97 -> 0.563 FAPH on DipCo (Manifest's default)
        } else if (x == "Moderately sensitive") {
          id(okay_nabu).set_probability_cutoff(176);    // 0.69 -> 0.376 FAPH on DipCo
          id(hey_jarvis).set_probability_cutoff(235);   // 0.92 -> 0.939 FAPH on DipCo
        } else if (x == "Very sensitive") {
          id(okay_nabu).set_probability_cutoff(143);    // 0.56 -> 0.751 FAPH on DipCo
          id(hey_jarvis).set_probability_cutoff(212);   // 0.83 -> 1.502 FAPH on DipCo
        }
