# Tools

Host-side and developer-only utilities live here.

- `mww_training_capture/` is the wake-word capture review service and Home Assistant add-on.
- `training/` contains helper scripts for preparing training datasets.

Nothing in this directory is part of the ESP32 firmware unless a config or build script explicitly references it.
